from backend.core.identity.auth.models import User
from backend.core.properties.portfolio.models import Property, PropertyImage
from backend.core.sales.crm.models import Contact, Note, Transaction, TransactionEvent, Document

# Add other models as they are created
